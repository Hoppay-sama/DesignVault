# Parameterized Template — Ethereal Dreamscape

> Use this template to generate an Ethereal Dreamscape page for YOUR brand.

---

```
Build an ethereal dreamscape page for "[BRAND_NAME]" — [ONE-LINE DESCRIPTION, e.g., "a luxury wellness brand rooted in calm"]. Stack: React + Vite + TypeScript + Tailwind CSS v4 + framer-motion. Page title: `[BRAND_NAME] — [TAGLINE]`.

Vibe: A scene from a dream. Soft pastel gradients bleed into each other as living gradient meshes. Elements float and drift as if weightless, wrapped in mist and soft glow. Aurora effects ripple slowly. Everything is soft: no hard edges, no sharp contrast, no saturated color. Motion is slow, continuous, barely perceptible.

---

## ANTI-SLOP CONSTRAINTS (DO NOT MODIFY)

- NO hard edges (everything blurs, diffuses, dissolves)
- NO sharp contrast (soft pastel palette only)
- NO grid-locked layout (elements float freely in space)
- NO bold saturated colors (low-saturation pastels only)
- NO fast, snappy motion (8-12s drift cycles minimum)
- NO heavy or bold type (light weights only)
- NO dense, solid blocks (translucent overlapping layers)
- NO flat, static backgrounds (gradient mesh IS the environment)

---

## FONTS

Replace [DISPLAY_FONT] with a light, airy typeface:
- Outfit (300-400) — airy, geometric, invisible
- Satoshi Light — premium, floating
- (Optional) a delicate serif like Cormorant Garamond (300) for an ethereal editorial feel

Load via Google Fonts. Use 300 for display, 400 for body. Light weights only — the text should float, not anchor. Generous letter-spacing (0.04em display, 0.25em labels).

---

## COLOR SYSTEM

```css
:root {
  --lavender: #E6E0F2;         /* Soft lavender */
  --blush-pink: #F5DEE3;       /* Blush pink */
  --powder-blue: #DCE7F5;      /* Powder blue */
  --cream: #FFF8EF;            /* Warm cream */
  --mint: #DFF0E8;             /* Pale mint */
  --glow-white: rgba(255, 253, 250, 0.55);   /* Soft glow wash */
  --text-soft: rgba(70, 66, 92, 0.9);        /* Muted plum-gray for text */
}
```

**Palette guidance:** Keep every color low-saturation and adjacent on the wheel:
- Lavender + powder blue — cool, celestial dreams
- Blush pink + cream — warm, tender dreams
- Mint + cream — fresh, airy dreams
Pick 3-4 pastels max, never more. All text in muted plum-gray.

---

## STRUCTURE

### Hero
- `min-h-[100dvh]`, gradient mesh background (your pastels, blurred blend)
- 3-4 floating soft shapes drifting at different altitudes (blurred orbs, mist)
- Aurora sweep: low-saturation gradient band, 18-24s cycle
- Text floating mid-air: label (uppercase, tracking 0.25em) + headline (`clamp(1.5rem, 3vw, 3rem)`, 300) + 1-2 sentence subtext + translucent pill CTA

### Gallery (3 panels)
- Each: `min-h-[100dvh]`, own gradient mesh + fog layers
- Transitions: dreamlike crossfade through white mist (2000ms)
- Text per panel: 1 label + 1 headline + 1-2 sentences (translucent, floating)

### About
- Split: gradient-mesh field + floating elements (left), translucent text (right)
- 2-3 sentences about softness, slowness, and atmosphere

### Footer
- Gradient mesh bg, faint text, minimal

---

## SOFT GLOW & MIST

```css
.glow-wash {
  background: radial-gradient(ellipse at 30% 20%, var(--glow-white), transparent 60%);
  filter: blur(16px);
}

.mist-layer {
  background: linear-gradient(to bottom, rgba(255,253,250,0.6), rgba(255,253,250,0) 70%);
  animation: mist-drift 22s ease-in-out infinite;
}

@keyframes mist-drift {
  0%, 100% { transform: translateX(-2%) scale(1); opacity: 0.5; }
  50% { transform: translateX(2%) scale(1.06); opacity: 0.8; }
}
```

---

## ANIMATIONS

- Gradient mesh: hue/position shift (aurora), 18-24s, infinite
- Floating elements: float (translateY + scale bob), 8-12s, infinite, staggered
- Text: opacity 0→1 + translateY, 1500-2000ms, ease-out (staggered delays)
- Section transitions: dreamlike crossfade through mist, 2000ms
- CTA: gentle float bob, 6s, infinite

`prefers-reduced-motion: reduce` → disable all drift/bob/aurora, instant entrance.

---

## VISUAL RULES (DO NOT MODIFY)

- Gradient mesh background — the environment, not a decoration
- Aurora effect — low-saturation color fields, slow shift (18-24s)
- Floating elements — weightless drift, nothing grid-locked
- Soft glow and light leaks — halation instead of shadows
- Mist/fog layers — atmospheric diffusion between sections
- Pastel palette only — 3-4 low-saturation colors max
- Translucent overlapping layers — dreamlike stacked depth
- Light sans-serif at 300/400 with generous letter-spacing
- Slow drift motion (8-12s), gentle parallax, breathing
- Soft grain over the whole page — unifies the fields
- No hard edges, no sharp contrast, no saturated color, no fast motion

---

## CONTENT GUIDANCE

**Headlines:** 4-8 words. Dreamlike, sensory. "A dream sequence, made weightless" not "We Make Wellness Apps."

**Body text:** 1-2 sentences. Evocative, hushed. Write like a dream journal entry, not a marketing pitch.

**Imagery:** Soft abstract renders — gradient meshes, floating fabric, mist, glass orbs with soft glow. Pastel palette only, low saturation, heavily diffused. No sharp product shots, no realistic photography.

---

Replace all [BRACKETED] placeholders, then paste into your AI coding assistant.
```
